import { VersionInfo } from '@start9labs/start-sdk'

export const v26_1_0__0_alpha_0 = VersionInfo.of({
  version: '26.1.0:0-alpha.0',
  releaseNotes: 'Initial release for StartOS',
  migrations: {
    up: async ({ effects }) => {},
    down: async ({ effects }) => {},
  },
})
